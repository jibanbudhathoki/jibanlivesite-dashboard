import React from 'react';
import { SkillsList } from '@/src/features/skills/components/SkillsList';

export function SkillsPage() {
  return (
    <div className="mx-auto max-w-7xl">
      <SkillsList />
    </div>
  );
}
